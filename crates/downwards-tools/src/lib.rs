//! Library half of `downwards-tools`: the soundtrack content-build pipeline
//! (`compose-tracks`, `render-track`) shared with its tests.

#![forbid(unsafe_code)]

pub mod music_inputs;
pub mod tracks;
